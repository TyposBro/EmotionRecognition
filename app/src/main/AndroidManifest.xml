<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lampa.emotionrecognition">

    <!-- Permissions -->
    <!-- READ_EXTERNAL_STORAGE is only needed for devices below Android 13 (API 33) for gallery access -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- CAMERA permission is essential for the live detection feature -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Declare camera feature requirements -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- FileProvider for taking photos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Main Activity (Legacy XML-based) -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="fullSensor"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- New Jetpack Compose Activity for Live Emotion Detection -->
        <activity
            android:name=".LiveEmotionComposeActivity"
            android:label="Live Emotion Detection"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar" /> <!-- A Compose-compatible theme -->

    </application>

</manifest>